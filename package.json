{
  "name": "takt",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.5.2",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently -n web,agent -c cyan,magenta \"pnpm --filter @takt/web dev\" \"pnpm --filter @takt/agent dev\"",
    "build": "pnpm -r --filter @takt/web build",
    "ingest": "pnpm --filter @takt/ingest start --",
    "profile:build": "pnpm --filter @takt/ingest profile:build --",
    "db:reset": "pnpm --filter @takt/db reset",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.7.3"
  }
}
